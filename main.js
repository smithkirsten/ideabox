var Idea = require('../ideabox/Idea');

// array for saved ideas

var savedIdeas = [];


// we will need a save function to push new options in to the new saved array

// we will need a delete function that uses event delligation

// we will need a cunction to create a new card that will be pushed into the saved grid